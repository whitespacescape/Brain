 declare module x{}
 declare module y{}
 declare module z{} 
 void function opq() {
      void (parseInt.toLocaleString)
      void (parseFloat.toString)
      void `this.o = this.x * this.y + this.z`
      void `this.p = this.x + this.y * this.z`
      void `this.q = this.y * this.z + this.x`
}
